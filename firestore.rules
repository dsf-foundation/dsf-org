rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null && request.auth.token.email == 'dsf2939@gmail.com';
    }

    match /heroes/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /blogs/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /galleryAlbums/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /galleryBatches/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}